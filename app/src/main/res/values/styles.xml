<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Page1" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/page1Primary</item>
        <item name="colorPrimaryDark">@color/page1PrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/page1Background</item>
    </style>

</resources>
